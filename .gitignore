# Gradle / Android build output
.gradle/
build/
app/build/
/captures
*.apk

# Local config (machine-specific)
local.properties

# Release signing credentials — never commit, see keystore.properties.example
keystore.properties

# Local quick-reference, mirrors app/build.gradle.kts versionCode/versionName
VERSION.txt

# Accumulated user-facing release notes; build.sh hands these off to the Desktop and clears it
releasenotes.txt

# IDE
.idea/
*.iml

# macOS
.DS_Store
build.sh
